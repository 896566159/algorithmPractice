滑动窗口模板：

int left = 0, right = 0;

while (right < s.size()) {
    // 增⼤窗⼝
    window.add(s[right]);
    right++;

    // 缩⼩窗⼝
    while (window needs shrink) {
        window.remove(s[left]);
        left++;
    }
 }


int left = 0;
int right = 0;
while (right < s.size()) {
    //	处理值

    while() { //判断符合条件
        // 最小在这里求

        // 左窗口移动
        left++;
    }

    // 最大在这里求
}
